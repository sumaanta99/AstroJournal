import {createNavigationContainerRef} from '@react-navigation/native';

import {RootStackTypes} from './RootStackTypes';

export const rootNavigationRef = createNavigationContainerRef<RootStackTypes>();
